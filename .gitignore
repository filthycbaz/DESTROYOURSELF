# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
#
# Unanchored so this also covers client/node_modules and server/node_modules
# in this monorepo layout (a leading "/" would only match the repo root).

# dependencies
node_modules/
.pnp
.pnp.js

# testing / coverage
coverage/
test-results/

# production builds
build/

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
.env

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Personal Claude Code settings — .claude/settings.json is the versioned, team-wide one
.claude/settings.local.json

# Cypress artifacts and local env overrides
client/cypress/videos/
client/cypress/screenshots/
client/cypress/downloads/
client/cypress.env.json
