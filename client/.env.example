# URL base de la API backend, incluyendo el prefijo /api.
# Obligatoria: si falta en un build de producción, la app falla al arrancar
# en el navegador en vez de apuntar silenciosamente a localhost.
REACT_APP_API_URL=http://localhost:3001/api
